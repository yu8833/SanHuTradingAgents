# TradingAgents-CN 开发与测试依赖
# 仅用于本地开发、CI、测试环境，不参与生产部署
# 安装方式：
#   pip install -r requirements.txt -r requirements-dev.txt

# ---- 测试框架 ----
pytest>=7.4.0
pytest-asyncio>=0.23.0
pytest-cov>=4.1.0
pytest-xdist>=3.5.0
httpx>=0.24.0
schemathesis>=3.25.0

# ---- 静态检查 ----
ruff>=0.6.0
mypy>=1.11.0
types-redis>=4.6.0
types-PyYAML>=6.0.0
types-requests>=2.32.0

# ---- Git 提交前钩子 ----
pre-commit>=3.8.0
