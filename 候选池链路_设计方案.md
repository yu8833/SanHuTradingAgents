# 三买三卖系统整体落地方案（菜单结构 × 候选池链路）

> 本文融合两部分：
> 1. **前端菜单结构**：以「三买三卖」为核心串起全站，形成「候选 → 自选 → 交易 → 分析 → 策略」的清晰主线。
> 2. **候选池链路设计**：「行业 → 个股 → 择时」三层流水线，解决当前用「三买三卖」直接全市场选股效果差的核心矛盾。

---

## 1. 背景与核心问题

当前系统用「三买三卖」`scan_three_buys_three_sells` 直接对全 A 股（约 5000 只）做单日快照扫描，把择时信号（B1/B2/B3/S）当作「选股」使用，效果不理想。

**本质原因：三买三卖是「择时层」，不是「选股层」。** 教材第十章明确三系统分工为「行业 → 个股 → 时机」决策链，三买三卖只是最后一环。当前系统缺少行业轮动（ETF Radar 等价物）和个股多因子（Stock Radar 等价物）两个上游层，导致：

- 信号噪声高、无差异化：5000 只票里任何一天都同时冒出大量 B2/B3，无法区分孰优。
- 信号被「单点化」：教材的 B1/B2/B3 是同一只票的连续持仓周期，扫描却只看最后一个 K 线。
- 风控（仓位/止损/持仓全红/利弗莫尔）依赖「已持仓」前提，纯扫描输出退化。

同时，前端菜单零散，模块各自为政，用户找不到「先用什么、再用什么」的路径。本方案一方面重构菜单以「三买三卖」为核心串起主线，另一方面把「三买三卖」降级为对上游候选池的择时确认，二者互为表里。

---

## 2. 目标链路（四层流水线）

```
┌─────────────────────────────────────────────────────────────┐
│ 第 0 层  大盘总开关（已有可复用）                              │
│   MarketRegimeDetector.detect()                              │
│   → 决定本期"选不选 / 降不降档"                               │
├─────────────────────────────────────────────────────────────┤
│ 第 1 层  行业/概念轮动（ETF Radar 等价物）                    │
│   → 选出强势行业/概念，得到行业强度分 sector_score            │
├─────────────────────────────────────────────────────────────┤
│ 第 2 层  个股多因子质量分（Stock Radar 等价物）                │
│   → 在强势行业内对个股打分排序，得到候选池 quality_score      │
├─────────────────────────────────────────────────────────────┤
│ 第 3 层  三买三卖择时 + 辅助信号确认 + ΔG 景气过滤           │
│   → 对候选池判"何时买卖"，并用辅助信号系统(教材第三章)        │
│     确认信号质量，输出可执行买入/卖出信号与建议仓位           │
└─────────────────────────────────────────────────────────────┘
```

**关键转变：三买三卖从「全市场选股」降级为「对上游候选池做择时确认」。**

---

## 3. 前端菜单结构（以三买三卖为核心）

### 3.1 完整菜单树

```
监控              ← 一级 · 默认落地页（系统速览）
市场              ← 一级
 ├─ 大盘看板
 ├─ 短线情绪
 ├─ 概念分析
 └─ 资讯
候选              ← 一级 · 行业→个股打分，自动更新
                  （Tab1 强势行业/概念 · Tab2 候选个股，按钮转自选）
自选              ← 一级
交易              ← 一级
 ├─ 模拟交易
 ├─ 持仓追踪
 ├─ 监控中心      ← 三买三卖规则引擎：买1监自选→买入；
                    买2/3、卖1/2/3监持仓→加减仓→清仓转自选
 └─ 交易复盘      ← 记录/梳理交易得失，复盘经验沉淀
分析              ← 一级
 ├─ 单股分析
 ├─ 批量分析
 └─ 分析报告
策略              ← 一级
 ├─ 三买三卖       ← 手动扫描/看信号（与监控中心自动执行互补）
 ├─ 常用策略       ← 一般策略 + 其他策略融合去重后的策略池（卡片式）
 ├─ 策略回测
 └─ 转债博弈       ← 资产类型独立（可转债），单独保留入口
任务              ← 一级 · 分析任务/队列
设置              ← 一级
 ├─ 个人设置：通用 / 外观 / 分析偏好 / 通知 / 安全 / 标签管理
 └─ 系统配置：配置管理 / 使用统计 / 缓存管理
    系统管理：用户管理 / 数据库管理 / 操作日志 / 系统日志 / 多数据源同步 / 定时任务
关于              ← 一级
```

### 3.2 菜单与现有路由的映射（可直接落地）

| 菜单项 | 路由 | 现有组件 / 变更 |
|---|---|---|
| 监控 | `/dashboard` | ✅ `views/Dashboard/index.vue`（现成） |
| 市场·大盘看板 | `/vibe/review/overview` | ✅ 现成 |
| 市场·短线情绪 | `/vibe/review/emotion` | ✅ 现成 |
| 市场·概念分析 | `/vibe/review/concept` | ✅ 现成 |
| 市场·资讯 | `/vibe/intel/radar` | ✅ 现成 |
| **候选·候选池** | `/candidate`（**新增**） | 🆕 `views/Candidate/index.vue`，Tab1/Tab2 |
| 自选 | `/favorites` | ✅ 现成 |
| 交易·模拟交易 | `/paper` | ✅ 现成 |
| 交易·持仓追踪 | `/portfolio` | ✅ 现成 |
| 交易·监控中心 | `/stock-alerts` | ✅ 现成（规则引擎承载点） |
| 交易·交易复盘 | `/paper/review` | 🆕 `views/PaperTrading/Review.vue`（记录/梳理交易得失） |
| 分析·单股分析 | `/analysis/single` | ✅ 现成 |
| 分析·批量分析 | `/analysis/batch` | ✅ 现成 |
| 分析·分析报告 | `/reports` | ✅ 现成 |
| 策略·三买三卖 | `/screening/three-buys-three-sells` | ✅ 现成（从选股移入策略） |
| 策略·常用策略 | `/screening/common` | ✅ 现成（StrategyScreener，融合后策略池） |
| 策略·策略回测 | `/screening/backtest` | ✅ 现成 |
| 策略·转债博弈 | `/screening/convertible-arbitrage` | 🟡 独立组件（资产类型特殊，保留） |
| 任务 | `/tasks` | ✅ 现成 |
| 设置·个人设置 | `/settings` 等 | ✅ 现成 |
| 设置·系统配置/系统管理 | `/settings/config` 等 | ✅ 现成 |
| 关于 | `/about` | ✅ 现成 |

### 3.3 菜单结构改动点

1. **「选股」更名为「策略」**，作为一级菜单，三买三卖置顶为第一项。
2. **「一般策略」与「其他策略」融合为「常用策略」**：去重、合并为统一卡片式策略池（沿用 StrategyScreener 表现层），删除重复独立组件，「转债博弈」因资产类型特殊单独保留。
3. **新增一级「候选」**（`/candidate`），作为流水线可视化出口，承接行业→个股打分。
4. 「策略·三买三卖」与「交易·监控中心」互补：前者手动扫描看信号，后者按规则引擎自动执行。
5. 菜单顺序：监控 → 市场 → **候选** → 自选 → 交易 → 分析 → 策略 → 任务 → 设置 → 关于。

### 3.4 前端改动文件清单

| 文件 | 改动 |
|---|---|
| `frontend/src/components/Layout/SidebarMenu.vue` | 重排菜单：新增「候选」、将「选股」改「策略」并三买三卖置顶、将「一般策略+其他策略」合为「常用策略」 |
| `frontend/src/router/index.ts` | 新增 `/candidate` 路由；将三买三卖等策略路由归入 `/screening` 下的「策略」语义；调整 redirect |
| `frontend/src/views/Candidate/index.vue` | 🆕 候选池页面（Tab1 强势行业/概念 + Tab2 候选个股） |
| `frontend/src/api/candidate.ts` | 🆕 候选池接口封装 |
| `frontend/src/views/Screening/StrategyScreener.vue` | 设置策略池由后端 `BUILTIN_STRATEGIES` 统一驱动，移除被合并的独立组件入口 |

### 3.5 策略融合：一般策略 × 其他策略（去重）

「一般策略」由后端 `app/strategy_system/strategies.py` 的 `BUILTIN_STRATEGIES`（12 个）驱动，是统一卡片式（StrategyScreener）的核心池；「其他策略」是 10 个独立 Vue 组件，各自为政、与一般策略多有重叠。融合原则：**以「一般策略」为核心，把「其他策略」中不重复、有价值的并入；重复的直接删除独立组件；策略回测沿用现有 `/screening/backtest` 方式（对策略池内任意策略发起回测）。**

#### 3.5.1 一般策略（核心池，保留）

| id | 名称 | 标签 |
|---|---|---|
| `ma_golden_cross` | MA金叉 | 均线/金叉 |
| `macd_golden` | MACD金叉 | MACD/金叉 |
| `n_day_high_breakout` | 创60日新高 | 突破/新高 |
| `n_day_low_reversal` | N日低点反转 | 反转/超跌 |
| `oversold_bounce` | 超跌反弹 | 超跌/反弹 |
| `trend_breakout` | 趋势突破 | 趋势/突破 |
| `boll_breakout` | 布林突破 | 布林/突破 |
| `volume_price_surge` | 量价齐升 | 量价/放量 |
| `pullback_ma20_bounce` | 回踩MA20反弹 | 均线/回踩 |
| `strong_open` | 强势高开 | 高开/强势 |
| `low_volatility_leader` | 低波动龙头 | 低波动/强势 |
| `low_pe_high_div_leader` | 低估值高股息龙头 | 价值/高股息 |

#### 3.5.2 其他策略（独立组件）去重对照

| 独立组件 | 语义 | 去重结论 | 处理 |
|---|---|---|---|
| `MaCrossover.vue` 均线交叉 | ↔ 核心池 `ma_golden_cross` | 重复 | 🔥 删除，卡池 `ma_golden_cross` 覆盖 |
| `VolumePrice.vue` 量价配合 | ↔ 核心池 `volume_price_surge` | 重复 | 🔥 删除，卡池 `volume_price_surge` 覆盖 |
| `LimitUpPullback.vue` 涨停回调 | 回踩反弹细类 | 重叠 | 🔥 删除，卡池 `pullback_ma20_bounce` 覆盖 |
| `MacdDivergence.vue` MACD背离 | 与 `macd_golden` 同源（MACD） | 高级变体 | 🟡 保留，折叠进常用策略池（不合并） |
| `ExtremeReversal.vue` 极端情绪反转 | 超跌反弹细类 | 高级变体 | 🟡 保留，折叠进常用策略池（不合并） |
| `Turnaround.vue` 困境反转 | 基本面反转（财务/ΔG） | **不重复** | ✅ 并入 → 新增卡池策略 `turnaround` |
| `SmallCapValue.vue` 小盘价值 | 市值因子选股 | **不重复** | ✅ 并入 → 新增卡池策略 `small_cap_value` |
| `ConvertibleArbitrage.vue` 转债博弈 | 资产类型独立（可转债） | 特殊 | 🟡 单独保留菜单入口 |
| `StrategyComparison.vue` 策略对比 | 工具（非策略） | 不并入 | 📌 保留为「策略回测」内嵌/折叠能力 |
| `RetailCenter.vue` 散户策略中心 | 聚合中心 | 不并入 | 📌 移除或折叠 |

> ✅ = 并入核心卡池；🟡 = 保留独立入口/折叠变体；🔥 = 删除；📌 = 工具/中心另行归置。

#### 3.5.3 融合后「适用策略」构成（确认版）

- **核心卡池**：`BUILTIN_STRATEGIES` 12 个 + 新增 `turnaround`（困境反转）+ `small_cap_value`（小盘价值）= **14 个统一卡片策略**，走 StrategyScreener 渲染，可统一筛选、回测、加自选。
- **高级变体（保留折叠）**：`macd_divergence`（MACD背离）、`extreme_reversal`（极端情绪反转），作为卡池内的高级子项，不与核心池合并。
- **独立入口**：`convertible_arbitrage`（转债博弈）单独保留菜单入口。
- **删除**：`ma_crossover`、`volume_price`、`limit_up_pullback` 三个独立组件及其路由。

- **新增策略 1「困境反转」**：财务/景气反转（复用 `financial_data_service` 的 `revenue_yoy/net_profit_yoy` + `dg_prosperity_service` 的 ΔG `reversal` 象限），在 `strategies.py` 新增 `def _turnaround` + `_def('turnaround', ...)`。
- **新增策略 2「小盘价值」**：市值因子（`total_mv` 10~50 亿区间）+ 低估值（`pe/pb`），在 `strategies.py` 新增 `def _small_cap_value` + `_def('small_cap_value', ...)`。

#### 3.5.4 策略回测方式（沿用现状）

不新增回测入口，直接复用现有 `/screening/backtest`（StrategyBacktest.vue）：对常用策略池内任意策略（含新增的 `turnaround`/`small_cap_value`）选择日期区间、参数、资金/仓位/止盈止损后发起回测，可异步落库、跨策略结果对比。融合只改「策略集合与入口」，不动回测引擎契约。

### 3.6 交易复盘（新增 · 记录/梳理交易得失）

「交易复盘」定位在「交易 · 监控中心」下方，是交易闭环的最后一环——把模拟交易执行后的得失沉淀下来，形成「决策 → 执行 → 复盘 → 改进」的经验飞轮。它由两部分组成：**自动成交记录**（来自模拟交易）+ **手动复盘笔记**（用户记录得失），两者结合。

#### 3.6.1 数据来源（复用现有）

| 数据 | 来源集合 | 说明 |
|---|---|---|
| 成交记录 | `paper_trades` | 每笔买卖，含 code/name/price/qty/金额/strategy/analysis_id |
| 持仓视角 | `paper_positions` | open/closed 状态，清仓后可形成完整一笔交易周期 |
| 订单 | `paper_orders` | 委托/成交状态 |
| 复盘笔记 | 🆕 `trade_reviews` | 用户手动记录的得失、经验（新增集合） |

#### 3.6.2 页面结构（两个面板）

```
交易复盘（落地页）
├─ 交易记录           ← 自动汇总：成交明细 + 已平仓交易周期（买入→卖出 + 盈亏）
└─ 复盘笔记           ← 手动记录：每笔交易的得失总结、经验教训
```

- **交易记录面板**：按已平仓交易聚合「一买一卖」为完整周期，展示 代码/名称/建仓价/平仓价/持仓天数/盈亏金额/盈亏率/所属策略；未平仓持仓单独展示。支持按策略、时间过滤。
- **复盘笔记面板**：为每笔交易（或任意成交流水）添加复盘笔记，字段含：盈亏归因、执行是否符合计划、经验教训、可改进点；支持标签、按交易关联。

#### 3.6.3 复盘笔记字段设计

| 字段 | 说明 |
|---|---|
| `trade_id` | 关联的成交/交易周期 ID（可空，表示自由记录） |
| `code / name` | 关联股票 |
| `strategy` | 所属策略（继承自 `paper_trades.strategy`） |
| `result` | 本次交易结果归因：`执行到位 / 止损及时 / 追高 / 割肉太早 / 踏空 / 其他` |
| `lesson` | 经验教训（文本） |
| `improvement` | 改进计划（文本） |
| `tags` | 复盘标签（如：纪律/心态/仓位/择时） |
| `created_at / updated_at` | 时间戳 |

#### 3.6.4 后端接口（新增）

| 接口 | 方法 | 说明 |
|---|---|---|
| `/api/paper/review/trades` | GET | 已平仓交易周期汇总（含盈亏），供交易记录面板 |
| `/api/paper/review/notes` | GET/POST | 复盘笔记 列表 / 新增 |
| `/api/paper/review/notes/{id}` | PUT/DELETE | 复盘笔记 更新 / 删除 |
| `/api/paper/review/stats` | GET | 复盘统计（胜率/盈亏比/各归因占比） |

统一响应格式：`{success, data, message}`（沿用现有契约）。

#### 3.6.5 前端改动清单

| 文件 | 改动 |
|---|---|
| `frontend/src/views/PaperTrading/Review.vue` | 🆕 交易复盘页（交易记录 + 复盘笔记两个面板） |
| `frontend/src/api/paper.ts` | 新增复盘相关接口封装 |
| `frontend/src/components/Layout/SidebarMenu.vue` | 交易一级下新增「交易复盘」菜单项 |
| `frontend/src/router/index.ts` | 新增 `/paper/review` 路由 |

#### 3.6.6 与三买三卖闭环的衔接

复盘笔记的 `result` 归因与「策略·三买三卖」信号、监控中心执行结果互为印证：监控中心自动执行产生的每笔交易，可在复盘页一键归因；多次复盘暴露的共性问题（如频繁追高、止损不及时），可反哺回策略参数或监控规则，形成「复盘 → 改进 → 再交易」的自我迭代。

---

## 4. 候选池页面设计（Tab 细化）

### 4.1 页面结构与 Tab 关系

```
候选（一级菜单）
└─ 候选池（落地页）
   ├─ Tab1  强势行业 / 概念        ← 第 1 层：行业轮动
   └─ Tab2  候选个股              ← 第 2 层：个股打分 + 第 3 层择时预览
```

- 默认进入 Tab1。Tab2 依赖 Tab1 选中的行业，未选行业时显示空态引导。
- 顶部常驻「大盘总开关」横幅（复用 MarketRegimeDetector）：
  - bull：正常展示；range：提示「择时退化，信号权重降低」；bear：提示「候选池上限减半，仅允许 B1 左侧小仓」。
- 顶部提供「加入自选」批量入口，把候选股一键转入自选池（`/favorites`）。

### 4.2 Tab1 强势行业 / 概念

| 维度 | 细化 |
|---|---|
| 数据源 | `industry_layer.get_industries(top_n)`（本地 `stock_basic_info.industry` 聚合成形股的动量/量能，合成 `sector_score`） |
| 排序口径 | 合成 `sector_score = 动量窗口加权 + 上行比例 + 量能`，默认按 `sector_score` 降序；可切换按「当日涨跌幅 / 资金净流入」 |
| 展示字段 | 行业名、`sector_score`、成分股数、代表个股（含涨跌幅/20日动量） |
| 过滤 | 「只看强势」开关（`sector_score` 阈值，默认 top 20）；「上行趋势」开关（要求 20 日动量均 > 0） |
| 交互 | 点击某行业 → 自动切到 Tab2 并携带该行业过滤；行内「成分股」按钮直接跳 Tab2 |
| 刷新 | financial 级缓存（日级/盘中），复用现 `concept_analysis` 缓存降级模式 |

可选叠加项：`get_concept_analysis` 返回的 `money_leaders`（资金净流入前 10）作为正向加分。

### 4.3 Tab2 候选个股

| 维度 | 细化 |
|---|---|
| 数据源 | Tab1 选中行业的成分股（缺口① `industry_members`）→ 多因子打分（缺口② `stock_score_layer`） |
| 打分因子 | 估值 `pe/pb`（行业中性化）、盈利 `roe`、规模 `total_mv`、景气 ΔG 象限、财务 `revenue_yoy/net_profit_yoy`、技术 `volume_ratio/amount` + `stock_analyzer.signal_score` |
| 硬过滤 | ΔG 位于 `double_kill`（戴维斯双杀）直接剔除；`unknown` 降权不剔除 |
| 择时预览 | 复用三买三卖单股检查，标注当前 B1/B2/B3/S 信号与建议仓位（第 3 层降级为「预览」）；叠加辅助信号质量分 `aux_score`（教材第三章，见 §6.3）对主信号做确认/降权/预警 |
| 展示字段 | 代码、名称、行业、`sector_score`、`quality_score`、ΔG 象限、择时信号、辅助信号，BIAS、建议仓位 |
| 排序 | 默认 `quality_score` 降序；可切换「按择时信号（有信号优先）」 |
| 交互 | 「加入自选」单排/批量；行点击 → 跳单股分析（`/analysis/single`） |
| 空态 | 未选行业：提示「请先在行业/概念 Tab 选择强势行业」；已选但无候选：提示当前行业景气/打分未通过 |
| 刷新 | 成分股日级（`industry_members`）；打分按请求实时，复用 `_batch_get_quotes` 批量取数避免逐股网络调用 |

---

## 5. 现状盘点（已具备 / 缺口）

| 环节 | 现有模块 | 状态 | 能否直接接入 |
|---|---|---|---|
| 大盘总开关 | `retail/market_regime_detector.py`（`MarketRegimeDetector.detect()`） | 已实现 | ✅ 直接复用 |
| 行业/概念强弱 | `concept_analysis.py`（概念实时 + RPS 轮动） | 已实现 | 🟡 可排序；候选池改用本地 `industry` 聚合 |
| 个股基本面 | `dg_prosperity_service.py`（ΔG 四象限） | 已实现 | ✅ 直接过滤 |
| 个股技术评分 | `stock_analyzer/stock_analyzer.py`（单股 100 分） | 已实现 | 🟡 小池子深度评分 |
| 条件筛选 | `enhanced_screening_service.py`、`screening_service.py` | 已实现 | 🟡 条件式，非自动打分 |
| 财务因子 | `financial_data_service.py`（revenue_yoy 等） | 已实现 | ✅ 可作基本面因子 |
| 择时 | `three_buys_three_sells_service.py` | 已实现 | ✅ 直接复用 |
| 行业→成分股映射 | `industry_layer`（本地 `stock_basic_info.industry` 反查） | ✅ 已落地 | ✅ 直接接入 |
| 全市场多因子打分（Stock Radar） | `stock_score_layer` | ✅ 已落地 | ✅ 直接接入 |
| 辅助信号系统（教材第三章） | `auxiliary_signal_layer` | ✅ 已落地 | ✅ 已接入候选池第 3 层（`aux_score`/预警） |
| 行业级 ΔG 景气 | `get_sector_dg()` | ✅ 已落地 | ✅ 直接接入（含修复 NaN/±inf 污染） |

已实现因子（`app/models/screening.py` 的 `BASIC_FIELDS_INFO`）：`total_mv / pe / pb / roe / volume_ratio / amount` 等，可作个股质量分的基础因子。

---

## 6. 架构设计

### 6.1 新增模块建议

建议新增一个编排服务 + 两个数据接口，不重构现有模块，尽量复用已有能力：

```
app/services/candidate_pool/
├── __init__.py
├── candidate_pool_service.py   # 编排：0→1→2→3 层流水线，对外输出候选池
│                               #   （第3层择时内联于 _attach_timing_preview）
├── industry_layer.py           # 第1层：行业轮动分数（本地 stock_basic_info.industry 聚合）
├── stock_score_layer.py        # 第2层：个股多因子打分（复用 screener + financial + ΔG）
└── (待实现) auxiliary_signal_layer.py
                                # 第3层辅助：教材第三章辅助信号系统（尚未落地，见 §6.3/§7.3）
```

> 说明：第 3 层择时目前内联在 `candidate_pool_service._attach_timing_preview`（复用 `three_buys_three_sells_service` 的 `scan_three_buys_three_sells` + 候选池限制），尚未拆分为独立的 `timing_layer.py`；辅助信号层 `auxiliary_signal_layer.py` 也尚未创建（部分信号雏形已存在于三买三卖服务，见 §6.3）。

### 6.2 各层职责与复用点

**第 0 层 大盘总开关**
- 复用 `MarketRegimeDetector.detect()`，输入 `(index_price, index_ma250, volatility_percentile, breadth_ratio, margin_balance_change_pct, turnover_ratio, turnover_ma20)`。
- 输出 `trend ∈ {bull, bear, range}`。
- 规则：`bear` 时整体降档（候选池上限减半、仅允许 B1 左侧小仓）；`range` 时提示择时退化，降低信号权重。候选池本期先不阻塞，仅返回 `trend` 供前端提示。

**第 1 层 行业/概念轮动**
- 复用 `industry_layer`：加载全市场日线面板 + 指标 + 基本面 enrichment，按本地 `stock_basic_info.industry` 聚合成形股的动量/量能，合成行业强度分 `sector_score`（不依赖网络）。
- 可选叠加：资金流榜（`get_concept_analysis().money_leaders`）作为正向加分。
- **缺口①**：行业/概念 → 成分股清单已由本地 `industry` 列反查实现（见 §7.1）。

**第 2 层 个股多因子打分**
- 输入：第 1 层产出的候选行业成分股清单。
- 因子（先做一版，后续可扩展）：
  - 估值：`pe / pb`（低分优，行业中性化）
  - 盈利能力：`roe`、`total_mv`（规模）
  - 成长/景气：ΔG 象限（`double_click` 加分、`double_kill` 直接剔除）
  - 财务：`revenue_yoy / net_profit_yoy`（来自 `financial_data_service`）
  - 技术/动量：`volume_ratio / amount`、短期趋势（复用 `stock_analyzer` 的 `signal_score`）
- 输出：`quality_score` 排序，截断为候选池（如 top 50）。
- **缺口②**：需实现一个将这些分散因子合成一个分数的「多因子打分器」（见 §7.2）。

**第 3 层 择时确认**
- 目前由 `candidate_pool_service._attach_timing_preview` 内联实现：传入 `pool=codes` 限制代码集，复用 `scan_three_buys_three_sells` 做择时预览（避免全市场 5000 只）。
- 三买三卖服务内部还提供 `_check_b1/b2/b3/s1/s2/s3` 单股检查与 `_precompute_indicators`，可作为后续把第 3 层抽出为独立 `timing_layer.py` 的复用点。
- 叠加 ΔG 硬过滤（`double_kill` 不买）。
- 待叠加**辅助信号系统**（教材第三章，见 §6.3）：量价/MACD/抄底/洗筹/密集突破/大盘-个股联动，对主信号做「确认/降权/预警」，合成辅助信号质量分 `aux_score`（尚未落地）。
- 输出：`{code, sector_score, quality_score, signal, aux_score, score, 建议仓位}`。
- 最终排序：`version A` 以 `quality_score` 为主、`signal` + `aux_score` 为辅；或 `version B` 以择时信号触发为准。

### 6.3 辅助信号系统（对齐教材第三章）

三买三卖六信号（B1/B2/B3/S1/S2/S3）是择时主系统。教材第三章定义了**辅助信号系统**，用于提高判断准确性——辅助信号**不独立产生买卖**，只作为主信号的「质量确认器」，在候选池第 3 层叠加使用。候选池链路将辅助信号落为 `auxiliary_signal_layer.py`。

| 辅助信号 | 教材定义（第三章） | 候选池用途（主信号确认） |
|---|---|---|
| **量价关系**（3.1） | 量价四象限：价涨量涨/价涨量跌/价跌量涨/价跌量跌；量价背离检测（近 5 日价均值升而量均值降 + 价在 MA60 上方） | 信号质量分级：量价齐升=健康（确认）；价涨量跌=量价背离（降权/预警减仓） |
| **MACD 确认**（3.2） | MACD(12,26,9) 金叉配合 B2；顶背离（价创 20 日新高而 DIF 未新高，常先于 S1 出现）；底背离 | MACD 金叉强化 B2 突破；顶背离在 S1 前提前预警；底背离配合 B1/抄底 |
| **抄底信号**（3.3） | 三天不新低（近 3 日低点高于前 10 日低点）+ 短期均线组(MA5/8/13)粘合向上（间距<2% 且 MA5/MA8 上升） | 配合 B1 左侧小仓确认底部形成 |
| **上影洗筹模型**（3.4） | 30 日内涨停 → 长上影(≥5%) → 横盘(日振幅<5% 持续≥5 日) → 短期均线粘合(<2%)拐头 → 放量中阳突破 | 强势股洗盘后再启动，为 B2/B3 提供增强确认 |
| **密集成交突破模型**（3.5） | 30 日内巨量(60 日最高量)换手 → 窄幅震荡均线上移 → 放量中阳突破中期均线 | 筹码换手充分后二波启动，强化 B2 突破 |
| **大盘-个股联动**（3.6） | 大盘趋势(指数/MA60/MA20/MA60 斜率) + 四象限联动矩阵（大盘↑/↓ × 个股↑/↓） | 上下文滤波：大盘逆风时个股信号降权；联动象限影响建议仓位（如 80-100%/30-50%/50-70%/0-30%） |

**预警信号（附录 B，只预警不触发买卖）**：量价背离（价涨量跌+MA60 上方，警惕减仓）、MACD 顶背离（价新高+DIF 不新高，预警见顶）、当弱不弱（大盘跌+个股涨，关注领涨）、当强不强（大盘涨+个股跌，可能有隐患）。

**落地方式**：
- `auxiliary_signal_layer.py`（**尚未创建**，本层为待落地项）对候选池内每只票计算上述辅助信号，复用三买三卖 `_precompute_indicators` 产出的指标（MA5/8/13/55/60、BIAS、VOL、MACD），避免重复计算。
- **已有雏形可优先复用**（在三买三卖服务中已实现，不必重写）：`_check_bottom_pickup`（抄底）、`_check_macd_divergence`（MACD 背离）、`_check_volume_price_divergence`（量价背离）。落地时先抽取/接线这些现成函数，再补上影洗筹、密集成交突破、大盘-个股联动等缺失项。
- 输出 `auxiliary: {qty_verification, macd, bottom_fishing, washout, dense_break, regime_quadrant}` 及综合分 `aux_score`（0-100）。
- 择时预览中「建议仓位」由主信号 + 辅助分数共同决定：主信号触发 + 辅助确认加分 → 正常/加仓；主信号触发但辅助量价背离/大盘逆风 → 降权/小仓；仅预警信号 → 不触发买卖。
- 大盘-个股联动复用第 0 层 `MarketRegimeDetector`，与个股择时信号合成联动象限。

---

## 7. 需新增的拼图

### 7.1 行业 → 成分股映射

- **数据源（已确认）**：本地 `stock_basic_info.industry` 字段反查（用户已确认替代 akshare `_cons_ths` 外部接口）。`industry_layer` 通过 `_get_enriched_target` 的 `industry` 列聚合成分股，口径与个股多因子打分天然对齐。
- ~~备选（未采用）~~：akshare 同花顺概念成分 `ak.stock_board_concept_cons_ths` —— 因依赖外部接口稳定性而放弃，仅作参考。
- 落库：`industry_members` 集合（`{industry, code, name}`）仍可作日级缓存，但目前第 1 版直接由 `stock_basic_info` 反查，未单独建集合。
- 口径：行业字段混有申万行业与概念式口径（约 190 个行业），与个股多因子打分（同屏使用 `industry` 列）天然对齐，故第 1 版以本地行业为准，不依赖网络（见 §11「口径」取舍）。

### 7.2 全市场多因子打分器

- `stock_score_layer.py` **已落地**，实现 `score_stock(code) -> quality_score`，各因子归一化后加权。
- 权重可配置（config），当前用一版经验权重，跑通后再调。
- 复用：
  - 估值/ROE/换手：`screening_service` / `database_screening_service` 的字段
  - 财务：`financial_data_service.get_latest_financial_data`
  - 景气：`dg_prosperity_service.get_quadrant_batch`
  - 技术：`stock_analyzer.analyze_stock`
- 注意：打分器需批量、避免每只票一次网络调用（复用三买三卖里 `_batch_get_quotes` 的批量取数思路）。

### 7.3 辅助信号层（对齐教材第三章）

- 新增 `auxiliary_signal_layer.py`（**待落地**），对候选池内每只票计算教材第三章的辅助信号：量价关系、MACD 确认、抄底信号、上影洗筹、密集成交突破，并叠加第 0 层大盘 regime 做大盘-个股联动。
- 复用三买三卖 `_precompute_indicators` 已产出的指标（MA5/8/13/55/60、BIAS、VOL、MACD），避免重复计算；复用 `MarketRegimeDetector` 取大盘趋势。
- **已有雏形复用**：三买三卖服务中的 `_check_bottom_pickup`（抄底）、`_check_macd_divergence`（MACD 背离）、`_check_volume_price_divergence`（量价背离）可直接接线，避免重写。
- 输出 `aux_score`（0-100）与 `auxiliary` 明细，与三买三卖主信号合成「建议仓位」，不改变主信号定义（只做确认/降权/预警）。

---

## 8. 数据采集与刷新

| 数据 | 来源 | 刷新 |
|---|---|---|
| 概念实时+RPS | akshare 同花顺 | 盘中/日级（现 concept_analysis 已缓存） |
| 行业成分股 | 本地 `stock_basic_info.industry` 反查 | 无外部依赖，随基本面快照刷新 |
| ΔG 景气 | Tushare `fina_indicator` | 季度（现 dg 已实现） |
| 财务因子 | Tushare | 季报期（现 financial_data_service 已实现） |
| 行情 | 现三买三卖批量取数 | 复用 |

### 数据来源与刷新对照（候选池）

| 数据 | Tab | 来源 | 刷新 | 状态 |
|---|---|---|---|---|
| 强势行业轮动 | Tab1 | `industry_layer`（本地 industry 聚合） | 随快照刷新 | ✅ 已落地 |
| 概念资金流/领涨 | Tab1 | `get_concept_analysis`（可选加分） | 日级/盘中缓存 | ✅ 现成 |
| 大盘 regime | 顶部 | `MarketRegimeDetector` | 实时 | ✅ 现成 |
| 行业 → 成分股 | Tab2 | 本地 `stock_basic_info.industry` 反查 | 随快照刷新 | ✅ 已落地（§7.1） |
| 个股多因子打分 | Tab2 | `stock_score_layer` | 按请求 | ✅ 已落地 |
| ΔG 景气过滤 | Tab2 | `get_quadrant_batch` | 季度 | ✅ 现成 |
| 财务/估值/技术因子 | Tab2 | screening + financial + stock_analyzer | 季报/实时 | ✅ 现成 |
| 择时信号预览 | Tab2 | `_attach_timing_preview`（复用 scan） | 实时 | ✅ 已落地 |
| 辅助信号确认 | Tab2 | `auxiliary_signal_layer` | 实时 | ⛔ 待落地（§6.3/§7.3） |

---

## 9. 后端接口设计（新增 API）

新建 `app/routers/candidate.py`（已落地），暴露给前端：

| 接口 | 方法 | 说明 |
|---|---|---|
| `/api/candidate/industries?top_n=20` | GET | Tab1：返回强势行业/概念 + `sector_score` + 成分股数 + 代表个股 |
| `/api/candidate/members?industry=xx` | GET | Tab1 预览：返回某行业成分股清单 |
| `/api/candidate/stocks?industry=xx&limit=30&with_timing=true` | GET | Tab2：对指定行业成分股多因子打分，返回候选个股（含 ΔG 过滤 + 择时预览；辅助信号 `aux_score` 待 §6.3 落地后接入） |
| `/api/candidate/favorites/batch` | POST | 批量加入自选池 `{items:[{code,name}]}` |
| **_ 无 `/refresh` 接口** | — | 行业成分股刷新走日级任务，未单独暴露手动刷新接口 |

统一响应格式：`{success, data, message}`（沿用现有契约）。

---

## 10. 落地步骤（分阶段）

### 阶段 0（可行性验证）✅ 已过
- 用现有接口手动跑一遍：大盘 regime → 强势行业/概念 → 取成分股 → 对成分股逐个跑 `stock_analyzer` + 三买三卖单股检查，确认候选池质量明显优于现全市场扫描。

### 阶段 1（最小改动，后置过滤）
- 保留现三买三卖全市场扫描为「初筛候选池」，新增一层组合过滤：`大盘非熊 + ΔG 非双杀 + 强势行业 top N`。
- 风险可控、不引新数据源，最快见效。

### 阶段 2（前端菜单重构 + 策略融合）✅ 已落地
- 已改 `SidebarMenu.vue` + `router/index.ts`：把「选股」改「策略」、「候选」一级、三买三卖置顶（见 §3）。
- 已执行策略融合（§3.5）：删除 `ma_crossover`/`volume_price`/`limit_up_pullback` 三个重复组件；折叠保留 `macd_divergence`/`extreme_reversal`；新增 `turnaround`/`small_cap_value` 后端策略；`convertible_arbitrage` 保留独立入口。

### 阶段 3（补行业→成分股，候选池页面随同上线）✅ 已落地
- 已实现 `industry_layer`（本地 `stock_basic_info.industry` 反查）+ `views/Candidate/index.vue`（Tab1/Tab2），打通「候选 → 自选」。
- 真正确立「行业 → 个股 → 择时」三层结构。

### 阶段 4（实现对教材的 Stock Radar 等价物）✅ 已落地
- 已实现 `stock_score_layer` 多因子打分器；**行业级 ΔG（`get_sector_dg`）已实现**（本地 `stock_basic_info.industry` 反查成分股 → 聚合平均 G/ΔG/四象限分布/主导象限），四层已全对齐教材。

### 阶段 4.5（辅助信号系统接入择时层）✅ 已落地
- 已实现 `auxiliary_signal_layer.py`（教材第三章辅助信号：量价关系/MACD确认/抄底/上影洗筹/密集成交突破/大盘-个股联动），对齐三买三卖核心检信号逻辑（B1/B2/B3/S1/S2/S3 定义与参数）。
- 已接线三买三卖服务已有雏形（`_check_bottom_pickup`/`_check_macd_divergence`/`_check_volume_price_divergence`），避免重写。
- 已在候选池第 3 层叠加辅助信号质量分 `aux_score`，与主信号合成「建议仓位」（确认/降权/预警），不改变主信号定义；候选池页已展示「辅助分」与「预警」列。

### 阶段 5（监控中心接入闭环）✅ 已落地
- ✅ 辅助信号（量价背离/MACD 顶背离/当弱不弱/当强不强等预警）已接入监控中心作为风控预警项（`type=aux` 规则，作用域限指定标的/自选股，见 `monitor_service`）。
- ✅ 「策略·三买三卖」信号与「交易·监控中心」规则引擎打通（`type=tbs` 规则）：买1 监自选→买入；买2/3、卖1/2/3 监持仓→加减仓→清仓转自选。
  - 监控服务新增 `scan_tbs_instructions()`：评估所有启用的 `tbs` 规则，解析其股票集合（symbols/watchlist/positions 作用域），复用 `scan_three_buys_three_sells` 一次扫描（关闭 ΔG/流动性/评分过滤，保留全部信号），按规则 `tbs_dir`（buy/sell/both）把 B1/B2/B3/S1/S2/S3 择时信号落为「待确认指令」`monitor_tbs_orders`；带同源 pending 去重与 `cooldown_seconds` 冷却。
  - 指令状态机：`pending → executed / cancelled / dismissed`。`executed` 走共享纸面交易入口 `execute_market_order`（`app/services/paper_executor.py`，与手动模拟交易同源，保证一致成交逻辑），买入写入 `paper_positions`（含止损/止盈/策略元数据），卖出减仓/清仓。
  - 新增接口（`app/routers/monitor.py`）：`GET /api/monitor/tbs/orders`、`POST /tbs/orders/{id}/execute|cancel|dismiss`；`POST /api/monitor/check` 可手动触发评估（含 TBS 扫描）。
  - 前端：`MonitorCenter.vue` 新增「三买三卖待确认指令」面板（待确认/已执行/全部筛选，执行/取消/忽略按钮）；规则表单新增 `tbs` 类型与「买卖方向」`tbs_dir` 选择（买入/卖出/双向）。
  - 部署验证：后端重启 + 前端重建镜像，容器健康；`tbs` 路由注册、规则 CRUD、扫描评估、前端面板渲染均验证通过。

### 阶段 5.1（内置三买三卖默认规则）✅ 已落地
- ✅ **三买三卖自动执行作为本系统核心内置规则**，默认启用、可修改、可关闭、**不可删除**（`builtin` 标志保护，`delete_rule` 拒绝）。
- ✅ 内置三条默认 `tbs` 规则（`DEFAULT_TBS_RULES` 模板，`ensure_default_rules` 按用户播种）：
  - `tbs_default_buy1`：买1（自选建仓）—— `scope=watchlist`、`tbs_dir=buy`、`tbs_signals=[B1]`；
  - `tbs_default_buy23`：买2/3（持仓加仓）—— `scope=positions`、`tbs_dir=buy`、`tbs_signals=[B2,B3]`；
  - `tbs_default_sell123`：卖1/2/3（持仓减仓/清仓）—— `scope=positions`、`tbs_dir=sell`、`tbs_signals=[S1,S2,S3]`。
- ✅ 内置规则 id 按用户作用域唯一（追加 `user_id` 哈希后缀，规避 DB `id` 唯一索引冲突）；`list_rules` 首访即播种并隔离其他用户规则。
- ✅ `scan_tbs_instructions` 按规则 `tbs_signals` 过滤信号（内置规则细分买1/买2/3/卖1/2/3，避免重复生成指令）。
- ✅ 前端：监控中心规则表单新增 `tbs` 专用配置（`tbs_dir` 方向 + `tbs_signals` 监听信号），三买三卖规则**免触发条件**（择时信号由系统自动计算）；内置规则带「内置」标签、删除按钮禁用。

---

## 11. 风险与取舍

- **口径统一（已确认本地行业）**：候选池第 1 版统一以本地 `stock_basic_info.industry` 为口径（约 190 个行业，混有申万行业与概念式），与个股多因子打分同屏使用 `industry` 列天然对齐，避免与概念轮动 join 失败。概念轮动仅作可选加分项。
- **数据源稳定性**：候选池主链路不依赖网络（行业成分股本地反查）；akshare 同花顺接口（概念资金流等可选加分项）可能受限/变更，需缓存 + 失败降级（沿用现有 `valid=lambda` 缓存降级模式）。
- **打分器过拟合**：因子权重属经验值，需按教材「一次只调一个参数、观察再评估」原则迭代，避免快速调参。
- **性能**：多因子打分若逐股网络调用会拖慢扫描，必须复用批量取数 + MongoDB 聚合，避免变成第二个「6 小时同步」。
- **三买三卖核心检信号逻辑需对齐教材**：本方案只改「上游怎么选、下游怎么用」，**三买三卖核心检信号逻辑需对齐** `/Users/yupeng/stock/策略/三买三卖交易系统_完整教材.pdf`——即 B1/B2/B3/S1/S2/S3 六信号的触发定义与参数（乖离率阈值 -20%/-30% 与 30%/65%/100%、放量 VOL>MA20×1.5、中阳线实体≥5%、跌破确认连续 2 日、MA60 斜率等）须与教材一致，避免破坏已有回测/数据契约。教材第三章的**辅助信号系统**作为候选池第 3 层的信号质量确认器叠加引入（见 §6.3），不改变主信号定义，只用于对主信号做「确认/降权/预警」。

---

## 12. 已确认项（用户决策）

> 以下为已确认的决策，直接作为落地依据。

1. **策略删除范围已确认**：删除独立组件 **均线交叉 / 量价配合 / 涨停回调** 3 个；**保留 MACD背离、极端情绪反转** 作为高级变体（折叠进常用策略池，不与核心池的 `macd_golden`/`oversold_bounce` 合并）；「转债博弈」单独保留入口。
2. **新增策略已确认**：融合后新增 `turnaround`（困境反转）与 `small_cap_value`（小盘价值）两个后端策略，并入常用策略池（在 `strategies.py` 新增 filter 与 `_def`）。
3. **候选池输出规模**：top 30。
4. **阶段2首版候选个股数据来源**：**不用**现三买三卖全市场扫描结果占位，候选池页面与行业成分股打分（阶段3）一起上线。
5. **行业口径已确认**：候选池行业成分股用本地 `stock_basic_info.industry` 反查（替代 akshare 概念成分股外部接口），不依赖网络，与个股打分同源。

## 13. 其余待确认项

1. 多因子权重是否接受一版「经验默认值」先跑通，再迭代？
2. 「策略」一级菜单下三买三卖置顶后，是否保留原有「选股」入口的跳转别名（如 `/screening` 仍重定向到三买三卖）？